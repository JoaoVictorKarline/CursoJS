const pessoa = {
    nome: 'João',
    email: 'josjojsdo@',
    idade: 12
}

const {nome, idade } = pessoa // Tire do objeto o atributo Nome e idade e criei uma variável pra ele

console.log(nome, idade)