const soma = (a, b) => { // Sinal => Substitui o nome function (Function arrow)
    return a+b
}

console.log(soma(2, 4))

//Retorno ímplicito

const sub = (a,b) => a-b //Executa apenas uma sentença

console.log(sub(3,2))